{{!
    Admin layout — LernHive app-shell (sidebar) + Moodle full_header (admin nav tree).

    The sidebar provides consistent top-level navigation (Home / Dashboard /
    MyCourses / SiteAdmin). full_header renders Moodle's secondary navigation
    which includes the admin settings category tree on admin pages.

    No block regions, no Context Dock on admin pages.
}}
{{> theme_boost/head }}
<body {{{ bodyattributes }}}>
{{> core/local/toast/wrapper}}
<div id="page-wrapper" class="theme-lernhive-shell">
    {{{ output.standard_top_of_body_html }}}
    <a class="lernhive-skip-link" href="#maincontent">Skip to main content</a>

    <div class="lernhive-app-shell">
        {{! LernHive sidebar — same as drawers.mustache }}
        {{> theme_lernhive/sidebar }}

        <div id="page" class="lernhive-page lernhive-admin-page">
            {{! full_header outputs the primary nav bar + Moodle secondary nav
                (admin settings category tree) for admin pagelayout pages. }}
            <div class="lernhive-admin-header">
                {{{ output.full_header }}}
            </div>

            <div id="page-content" class="lernhive-page-content">
                <main id="region-main-box" class="lernhive-main">
                    <span id="maincontent" class="lernhive-skip-anchor"></span>
                    <div id="region-main" class="lernhive-main-section lernhive-admin-content">
                        {{#hasregionmainsettingsmenu}}
                            <div class="lernhive-settings-menu">
                                {{{ regionmainsettingsmenu }}}
                            </div>
                        {{/hasregionmainsettingsmenu}}
                        {{{ output.course_content_header }}}
                        {{{ output.main_content }}}
                        {{{ output.activity_navigation }}}
                        {{{ output.course_content_footer }}}
                    </div>
                </main>
            </div>

            {{> theme_lernhive/footer }}
        </div>
    </div>

    {{{ output.standard_after_main_region_html }}}
    {{{ output.standard_end_of_body_html }}}
</div>
</body>
</html>
